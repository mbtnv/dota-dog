# Settings for the self-hosted deploy runner.
# Copy this file to /opt/dota-dog/deploy.env on the server and point DEPLOY_DIR
# to the existing git checkout that you already deploy from manually.

DEPLOY_DIR=/absolute/path/to/existing/dota-dog
DEPLOY_BRANCH=main
DEPLOY_COMPOSE_FILE=docker-compose.yml
